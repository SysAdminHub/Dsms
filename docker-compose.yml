services:
  mysql:
    image: mysql:8.0
    container_name: dsms-mysql
    ports:
      - "3306:3306"
    environment:
      MYSQL_ROOT_PASSWORD: changeme
      MYSQL_DATABASE: dsms_dev
    volumes:
      - dsms_mysql_data:/var/lib/mysql

volumes:
  dsms_mysql_data:
